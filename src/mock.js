import { mkdir, writeFile } from 'node:fs/promises'; import path from 'node:path';
export async function renderMock(request,outDir){await mkdir(outDir,{recursive:true});const segments=[];for(const s of request.segments){const meta={id:s.id,purpose:s.purpose,status:'mocked',asset:null,script:s.script};await writeFile(path.join(outDir,`${s.id}.json`),JSON.stringify(meta,null,2));segments.push(meta);}const manifest={version:'1.0',projectId:request.projectId,mode:'mock',status:'RENDERED',segments};await writeFile(path.join(outDir,'presenter-manifest.json'),JSON.stringify(manifest,null,2));return manifest;}
