#!/usr/bin/env node
import { readFile } from 'node:fs/promises';import path from 'node:path';import process from 'node:process';import { validatePresenterRequest } from '../src/validate.js';import { estimateCost } from '../src/cost.js';import { renderMock } from '../src/mock.js';import { renderLive } from '../src/live.js';
async function load(file){return JSON.parse(await readFile(file,'utf8'));}
async function main(){const[cmd,file,...flags]=process.argv.slice(2);if(cmd==='doctor'){console.log(JSON.stringify({node:process.version,platform:process.platform,apiKeyConfigured:Boolean(process.env.HEYGEN_API_KEY)},null,2));return;}if(!file)throw new Error('usage: cano-heygen <validate|estimate|render> request.json [--mock|--live]');const request=await load(file);const check=validatePresenterRequest(request);if(!check.ok)throw new Error(check.errors.join('; '));if(cmd==='validate'){console.log(JSON.stringify({ok:true,projectId:request.projectId},null,2));return;}if(cmd==='estimate'){console.log(JSON.stringify(estimateCost(request),null,2));return;}if(cmd==='render'){const profile=await load(path.resolve(request.profile));const out=path.resolve('.runtime','jobs',request.projectId,'presenter');const result=flags.includes('--live')?await renderLive(request,profile,out):await renderMock(request,out);console.log(JSON.stringify({...result,outDir:out,cost:estimateCost(request)},null,2));return;}throw new Error(`unknown command: ${cmd}`);}main().catch(e=>{console.error(e.message);process.exitCode=1;});
